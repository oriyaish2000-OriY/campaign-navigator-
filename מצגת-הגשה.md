<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Share-Pen — מצגת הגשה</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;0,700;1,300;1,400;1,600&family=Heebo:wght@300;400;500;700&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --dark:    #060408;
      --dark2:   #0d0b10;
      --dark3:   #161219;
      --gold:    #d4a535;
      --gold-lt: #e8c060;
      --gold-dk: #a07820;
      --white:   #f0ead8;
      --muted:   #7a7068;
    }

    html, body {
      height: 100%;
      overflow: hidden;
      background: var(--dark);
      color: var(--white);
      font-family: 'Heebo', sans-serif;
    }

    /* ══ SLIDE CONTAINER ══ */
    .deck {
      width: 100%;
      height: 100vh;
      position: relative;
      overflow: hidden;
    }

    .slide {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 3rem 4rem;
      opacity: 0;
      transform: translateX(-60px);
      transition: opacity 0.55s cubic-bezier(0.4,0,0.2,1),
                  transform 0.55s cubic-bezier(0.4,0,0.2,1);
      pointer-events: none;
      overflow-y: auto;
    }
    .slide.active {
      opacity: 1;
      transform: translateX(0);
      pointer-events: all;
    }
    .slide.exit-left {
      opacity: 0;
      transform: translateX(60px);
    }

    /* DOT GRID per slide */
    .slide::before {
      content: '';
      position: absolute;
      inset: 0;
      background-image: radial-gradient(circle, rgba(212,165,53,0.06) 1px, transparent 1px);
      background-size: 32px 32px;
      pointer-events: none;
      z-index: 0;
    }

    .slide-inner {
      position: relative;
      z-index: 1;
      max-width: 960px;
      width: 100%;
    }

    /* ══ NAV ══ */
    .nav {
      position: fixed;
      bottom: 2rem;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      align-items: center;
      gap: 1.5rem;
      z-index: 100;
      background: rgba(6,4,8,0.85);
      backdrop-filter: blur(8px);
      border: 1px solid rgba(212,165,53,0.2);
      border-radius: 100px;
      padding: 0.6rem 1.4rem;
    }
    .nav-btn {
      background: none;
      border: none;
      color: var(--gold);
      font-size: 1.2rem;
      cursor: pointer;
      padding: 0.3rem 0.5rem;
      transition: opacity 0.2s;
      font-family: 'Heebo', sans-serif;
    }
    .nav-btn:hover { opacity: 0.7; }
    .nav-btn:disabled { opacity: 0.25; cursor: default; }
    .counter {
      font-size: 0.8rem;
      font-weight: 500;
      letter-spacing: 0.15em;
      color: rgba(212,165,53,0.7);
      min-width: 3rem;
      text-align: center;
    }
    .dots {
      display: flex;
      gap: 0.45rem;
      align-items: center;
    }
    .dot {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: rgba(212,165,53,0.25);
      cursor: pointer;
      transition: background 0.2s, transform 0.2s;
    }
    .dot.active {
      background: var(--gold);
      transform: scale(1.4);
    }

    /* ══ COMMON ELEMENTS ══ */
    .eyebrow {
      font-size: 0.68rem;
      font-weight: 500;
      letter-spacing: 0.45em;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 1.2rem;
    }
    .slide-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 0.85rem;
      color: rgba(212,165,53,0.35);
      letter-spacing: 0.15em;
      margin-bottom: 0.5rem;
    }
    h1.hero {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(3rem, 7vw, 6rem);
      font-weight: 300;
      line-height: 1.0;
      color: var(--white);
    }
    h1.hero em { color: var(--gold); font-style: italic; }
    h2.heading {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2rem, 4vw, 3.2rem);
      font-weight: 400;
      line-height: 1.15;
      color: var(--white);
      margin-bottom: 1.5rem;
    }
    h2.heading em { color: var(--gold); font-style: italic; }
    .body-text {
      font-size: 1rem;
      font-weight: 300;
      color: rgba(240,234,216,0.75);
      line-height: 1.8;
    }
    .gold-rule {
      width: 50px;
      height: 2px;
      background: var(--gold);
      margin: 1.8rem 0;
    }

    /* ══ SLIDE 1 — TOPIC ══ */
    #slide-1 {
      background: radial-gradient(ellipse at 70% 40%, rgba(212,165,53,0.07) 0%, transparent 60%),
                  var(--dark);
    }
    .hero-tagline {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.1rem, 2vw, 1.5rem);
      font-style: italic;
      color: var(--gold-lt);
      margin-top: 0.8rem;
    }
    .hero-body {
      font-size: 1rem;
      color: rgba(240,234,216,0.65);
      line-height: 1.75;
      max-width: 560px;
      margin-top: 2rem;
    }
    .pen-globe {
      position: absolute;
      top: 50%;
      left: 3rem;
      transform: translateY(-50%);
      font-size: clamp(6rem, 14vw, 12rem);
      opacity: 0.08;
      pointer-events: none;
      line-height: 1;
      user-select: none;
    }
    .inspiration-pills {
      display: flex;
      flex-wrap: wrap;
      gap: 0.7rem;
      margin-top: 2rem;
    }
    .pill {
      background: rgba(212,165,53,0.1);
      border: 1px solid rgba(212,165,53,0.25);
      border-radius: 100px;
      padding: 0.35rem 1rem;
      font-size: 0.82rem;
      color: var(--gold-lt);
      letter-spacing: 0.04em;
    }

    /* ══ SLIDE 2 — BRAND IDENTITY ══ */
    #slide-2 { background: var(--dark2); }
    .brand-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2.5rem;
      margin-top: 1.5rem;
    }
    .brand-logo-display {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 300;
      letter-spacing: 0.12em;
      color: var(--gold);
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }
    .brand-logo-display span { color: var(--white); }
    .brand-subtitle {
      font-size: 0.75rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--muted);
    }
    .swatches {
      display: flex;
      flex-wrap: wrap;
      gap: 0.7rem;
      margin-top: 1rem;
    }
    .swatch {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.4rem;
    }
    .swatch-circle {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      border: 1px solid rgba(255,255,255,0.1);
    }
    .swatch-code {
      font-size: 0.62rem;
      letter-spacing: 0.05em;
      color: var(--muted);
    }
    .type-specimens {
      margin-top: 1rem;
    }
    .specimen-row + .specimen-row { margin-top: 0.7rem; }
    .specimen-label {
      font-size: 0.65rem;
      letter-spacing: 0.25em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 0.2rem;
    }
    .specimen-cormorant {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.8rem;
      font-weight: 300;
      color: var(--white);
      line-height: 1.1;
    }
    .specimen-heebo {
      font-family: 'Heebo', sans-serif;
      font-size: 1rem;
      font-weight: 400;
      color: rgba(240,234,216,0.7);
    }
    .voice-tags {
      display: flex;
      gap: 0.8rem;
      flex-wrap: wrap;
      margin-top: 0.8rem;
    }
    .voice-tag {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-style: italic;
      color: var(--gold-lt);
    }
    .voice-tag + .voice-tag::before {
      content: '·';
      margin-left: 0.8rem;
      color: var(--muted);
      font-style: normal;
    }

    /* ══ SLIDE 3 — MATERIALS ══ */
    #slide-3 { background: var(--dark); }
    .materials-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin-top: 1.5rem;
    }
    .mat-item {
      background: rgba(212,165,53,0.05);
      border: 1px solid rgba(212,165,53,0.15);
      border-radius: 10px;
      padding: 1rem 1.1rem;
      transition: border-color 0.2s;
    }
    .mat-item:hover { border-color: rgba(212,165,53,0.35); }
    .mat-icon {
      font-size: 1.4rem;
      margin-bottom: 0.4rem;
    }
    .mat-title {
      font-family: 'Heebo', sans-serif;
      font-weight: 500;
      font-size: 0.88rem;
      color: var(--gold-lt);
      margin-bottom: 0.2rem;
    }
    .mat-detail {
      font-size: 0.75rem;
      color: var(--muted);
      line-height: 1.4;
    }

    /* ══ SLIDE 4 — EXHIBITION ══ */
    #slide-4 {
      background: radial-gradient(ellipse at 30% 60%, rgba(212,165,53,0.05) 0%, transparent 55%),
                  var(--dark2);
    }
    .zones-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0.9rem;
      margin-top: 1.5rem;
    }
    .zone-card {
      background: rgba(6,4,8,0.6);
      border: 1px solid rgba(212,165,53,0.12);
      border-radius: 10px;
      padding: 1rem;
      text-align: center;
      transition: border-color 0.2s;
    }
    .zone-card:hover { border-color: rgba(212,165,53,0.3); }
    .zone-num {
      font-family: 'Cormorant Garamond', serif;
      font-size: 2rem;
      color: rgba(212,165,53,0.3);
      line-height: 1;
      margin-bottom: 0.3rem;
    }
    .zone-name {
      font-size: 0.82rem;
      font-weight: 500;
      color: var(--gold-lt);
      margin-bottom: 0.3rem;
    }
    .zone-desc {
      font-size: 0.72rem;
      color: var(--muted);
      line-height: 1.4;
    }
    .curatorial {
      margin-top: 1.5rem;
      background: rgba(212,165,53,0.05);
      border-right: 2px solid var(--gold);
      padding: 0.8rem 1.2rem;
      border-radius: 0 8px 8px 0;
    }
    .curatorial p {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1rem;
      font-style: italic;
      color: rgba(240,234,216,0.7);
      line-height: 1.6;
    }

    /* ══ SLIDE 5 — VISITOR ══ */
    #slide-5 { background: var(--dark); }
    .journey-row {
      display: flex;
      gap: 0;
      margin-top: 2rem;
      overflow-x: auto;
    }
    .journey-step {
      flex: 1;
      min-width: 120px;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      position: relative;
    }
    .journey-step:not(:last-child)::after {
      content: '';
      position: absolute;
      top: 22px;
      left: calc(50% + 22px);
      width: calc(100% - 44px);
      height: 1px;
      background: rgba(212,165,53,0.25);
    }
    .j-circle {
      width: 44px;
      height: 44px;
      border-radius: 50%;
      border: 1.5px solid var(--gold);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      margin-bottom: 0.7rem;
      background: rgba(212,165,53,0.08);
    }
    .j-label {
      font-size: 0.75rem;
      font-weight: 500;
      color: var(--gold-lt);
      margin-bottom: 0.3rem;
    }
    .j-desc {
      font-size: 0.68rem;
      color: var(--muted);
      line-height: 1.4;
      padding: 0 0.3rem;
    }
    .merch-row {
      display: flex;
      gap: 1.2rem;
      margin-top: 2rem;
    }
    .merch-card {
      flex: 1;
      background: rgba(212,165,53,0.05);
      border: 1px solid rgba(212,165,53,0.15);
      border-radius: 10px;
      padding: 1rem 1.2rem;
    }
    .merch-card-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.05rem;
      color: var(--gold);
      margin-bottom: 0.3rem;
    }
    .merch-card-detail {
      font-size: 0.78rem;
      color: var(--muted);
      line-height: 1.5;
    }

    /* ══ SLIDE 6 — AI & CONCLUSION ══ */
    #slide-6 {
      background: radial-gradient(ellipse at 50% 50%, rgba(212,165,53,0.06) 0%, transparent 65%),
                  var(--dark2);
    }
    .ai-tools-row {
      display: flex;
      gap: 1.2rem;
      margin-top: 1.5rem;
      flex-wrap: wrap;
    }
    .ai-tool {
      flex: 1;
      min-width: 160px;
      background: rgba(6,4,8,0.7);
      border: 1px solid rgba(212,165,53,0.18);
      border-radius: 12px;
      padding: 1.2rem;
      text-align: center;
    }
    .ai-tool-icon { font-size: 1.8rem; margin-bottom: 0.5rem; }
    .ai-tool-name {
      font-weight: 500;
      font-size: 0.9rem;
      color: var(--gold-lt);
      margin-bottom: 0.3rem;
    }
    .ai-tool-role { font-size: 0.75rem; color: var(--muted); }
    .learning-block {
      margin-top: 1.8rem;
      background: rgba(212,165,53,0.06);
      border: 1px solid rgba(212,165,53,0.2);
      border-radius: 12px;
      padding: 1.4rem 1.8rem;
    }
    .learning-block p {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1rem, 2vw, 1.3rem);
      font-style: italic;
      color: rgba(240,234,216,0.8);
      line-height: 1.6;
    }
    .learning-block p strong { color: var(--gold); font-style: normal; }
    .final-quote {
      margin-top: 1.8rem;
      text-align: center;
    }
    .final-quote .quote-text {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(1.5rem, 3vw, 2.2rem);
      font-weight: 400;
      font-style: italic;
      color: var(--white);
      line-height: 1.35;
    }
    .final-quote .quote-text em { color: var(--gold); }
    .cta-link {
      display: inline-block;
      margin-top: 1.2rem;
      font-size: 0.8rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--gold);
      text-decoration: none;
      border-bottom: 1px solid rgba(212,165,53,0.35);
      padding-bottom: 2px;
    }
  </style>
</head>
<body>

<div class="deck" id="deck">

  <!-- ══════════════════════════════════════ SLIDE 1 ══════════════════════════════════════ -->
  <div class="slide active" id="slide-1">
    <div class="pen-globe" aria-hidden="true">✒</div>
    <div class="slide-inner">
      <div class="slide-num">01 / 06</div>
      <div class="eyebrow">The Theme · הנושא</div>
      <h1 class="hero">
        SHARE-PEN<br />
        <em>The Writing</em><br />
        Revolution
      </h1>
      <div class="hero-tagline">מהפכת הכתיבה</div>
      <div class="gold-rule"></div>
      <div class="hero-body">
        <strong style="color:var(--white);">למה הנושא הזה?</strong><br />
        טכנולוגיה כשחרור, לא כמגבלה. בעולם שבו AI מאיים על יצירתיות,
        Share-Pen מוכיח את ההפך: AI כשותף, לא כמחליף.
        ה"מה אם?" שהניע את הכל: <em style="color:var(--gold-lt);">מה אם כל אדם יוכל לספר את הסיפור שלו?</em>
      </div>
      <div class="inspiration-pills">
        <span class="pill">✦ Technology as Liberation</span>
        <span class="pill">✦ טכנולוגיה כשחרור</span>
        <span class="pill">✦ Write with Purpose</span>
        <span class="pill">✦ Not AI Instead of You</span>
      </div>
    </div>
  </div>

  <!-- ══════════════════════════════════════ SLIDE 2 ══════════════════════════════════════ -->
  <div class="slide" id="slide-2">
    <div class="slide-inner">
      <div class="slide-num">02 / 06</div>
      <div class="eyebrow">Brand Identity · זהות מותג</div>
      <h2 class="heading">
        The Visual Language —<br /><em>שפת הויזואל</em>
      </h2>
      <div class="brand-grid">
        <div>
          <div class="brand-logo-display"><span>SHARE</span>-PEN</div>
          <div class="brand-subtitle">Write with Purpose · כתוב בכוונה</div>
          <div class="gold-rule" style="margin:1.2rem 0 1rem;"></div>
          <div class="specimen-label">Color Palette</div>
          <div class="swatches">
            <div class="swatch">
              <div class="swatch-circle" style="background:#060408;border-color:rgba(255,255,255,0.2);"></div>
              <div class="swatch-code">#060408</div>
            </div>
            <div class="swatch">
              <div class="swatch-circle" style="background:#0e0c10;"></div>
              <div class="swatch-code">#0e0c10</div>
            </div>
            <div class="swatch">
              <div class="swatch-circle" style="background:#d4a535;"></div>
              <div class="swatch-code">#d4a535</div>
            </div>
            <div class="swatch">
              <div class="swatch-circle" style="background:#e8c060;"></div>
              <div class="swatch-code">#e8c060</div>
            </div>
            <div class="swatch">
              <div class="swatch-circle" style="background:#a07820;"></div>
              <div class="swatch-code">#a07820</div>
            </div>
            <div class="swatch">
              <div class="swatch-circle" style="background:#f0ead8;"></div>
              <div class="swatch-code">#f0ead8</div>
            </div>
          </div>
        </div>
        <div>
          <div class="type-specimens">
            <div class="specimen-row">
              <div class="specimen-label">Cormorant Garamond — Headlines</div>
              <div class="specimen-cormorant">The Story Begins Here</div>
              <div class="specimen-cormorant" style="font-style:italic; color:var(--gold);">הסיפור מתחיל כאן</div>
            </div>
            <div class="specimen-row" style="margin-top:1.2rem;">
              <div class="specimen-label">Heebo — Body Text</div>
              <div class="specimen-heebo">כתיבה עם מטרה. Writing with purpose.</div>
            </div>
          </div>
          <div class="gold-rule" style="margin:1.2rem 0 0.8rem;"></div>
          <div class="specimen-label">Graphic Language</div>
          <div style="font-size:0.85rem; color:rgba(240,234,216,0.65); line-height:1.6;">
            Dot grid · Gold hairlines · Dark drama<br />
            Letterpress textures · Ink bleeds
          </div>
          <div class="gold-rule" style="margin:1rem 0 0.8rem;"></div>
          <div class="specimen-label">Brand Voice</div>
          <div class="voice-tags">
            <span class="voice-tag">Revolutionary</span>
            <span class="voice-tag">Warm</span>
            <span class="voice-tag">Empowering</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ══════════════════════════════════════ SLIDE 3 ══════════════════════════════════════ -->
  <div class="slide" id="slide-3">
    <div class="slide-inner">
      <div class="slide-num">03 / 06</div>
      <div class="eyebrow">Marketing Materials · חומרי שיווק</div>
      <h2 class="heading">
        The Arsenal —<br /><em>הארסנל השיווקי</em>
      </h2>
      <div class="materials-grid">
        <div class="mat-item">
          <div class="mat-icon">🌐</div>
          <div class="mat-title">5 Landing Pages</div>
          <div class="mat-detail">EN · HE · ES · FR · DE — בדף נחיתה ייעודי לכל שוק</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">📱</div>
          <div class="mat-title">Social Media Sets</div>
          <div class="mat-detail">5 ערכות לינגואיסטיות — Instagram, LinkedIn, TikTok</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">🎬</div>
          <div class="mat-title">40 Video Scripts</div>
          <div class="mat-detail">סקריפטים לוידאו — תסריטים לכל פלטפורמה ושוק</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">🖼</div>
          <div class="mat-title">Main Poster 70×100</div>
          <div class="mat-detail">פוסטר הגלריה הראשי — dark gold, גדול פורמט</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">✉️</div>
          <div class="mat-title">Digital Invitation</div>
          <div class="mat-detail">הזמנה דיגיטלית לתערוכה — HTML מלא לשליחה</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">🖥</div>
          <div class="mat-title">Web Banner</div>
          <div class="mat-detail">באנר ווב אנימטד — פורמטים מרובים</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">📧</div>
          <div class="mat-title">5-Email Nurture</div>
          <div class="mat-detail">רצף 5 מיילים — onboarding → conversion → community</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">📣</div>
          <div class="mat-title">Ad Copy — All Languages</div>
          <div class="mat-detail">קופירייטינג לפרסום — Google Ads, Meta, LinkedIn</div>
        </div>
        <div class="mat-item">
          <div class="mat-icon">🎟</div>
          <div class="mat-title">Entry Ticket Design</div>
          <div class="mat-detail">כרטיס כניסה לתערוכה — gold foil design</div>
        </div>
      </div>
    </div>
  </div>

  <!-- ══════════════════════════════════════ SLIDE 4 ══════════════════════════════════════ -->
  <div class="slide" id="slide-4">
    <div class="slide-inner">
      <div class="slide-num">04 / 06</div>
      <div class="eyebrow">Exhibition Space · מרחב התערוכה</div>
      <h2 class="heading">
        The World We Built —<br /><em>העולם שבנינו</em>
      </h2>
      <div class="zones-grid">
        <div class="zone-card">
          <div class="zone-num">I</div>
          <div class="zone-name">The Threshold</div>
          <div class="zone-desc">שלט הכניסה — פתיחת המניפסט, מסדרון ה-GENESIS</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">II</div>
          <div class="zone-name">The Blank Page</div>
          <div class="zone-desc">אזור "הדף הלבן" — הפחד שShare-Pen מנצח</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">III</div>
          <div class="zone-name">The Co-Write Room</div>
          <div class="zone-desc">חדר ניסוי — שותפות AI בפעולה, בזמן אמת</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">IV</div>
          <div class="zone-name">The Voices</div>
          <div class="zone-desc">5 שפות, 5 קהילות — קולות מ-5 יבשות</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">V</div>
          <div class="zone-name">The Manifesto Wall</div>
          <div class="zone-desc">קיר המניפסט — GENESIS מודפס בפורמט ענק</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">VI</div>
          <div class="zone-name">The Market</div>
          <div class="zone-desc">חנות הפריטים — merchandise, Writing Set, חוויה</div>
        </div>
        <div class="zone-card">
          <div class="zone-num">VII</div>
          <div class="zone-name">The Exit Quote</div>
          <div class="zone-desc">יציאה עם ציטוט — "The revolution will be co-written"</div>
        </div>
      </div>
      <div class="curatorial">
        <p>
          "המרחב אינו תערוכה — הוא טקס כניסה לתנועה.
          כל אזור הוא פרק בסיפור; המבקר הוא הגיבור."
        </p>
      </div>
    </div>
  </div>

  <!-- ══════════════════════════════════════ SLIDE 5 ══════════════════════════════════════ -->
  <div class="slide" id="slide-5">
    <div class="slide-inner">
      <div class="slide-num">05 / 06</div>
      <div class="eyebrow">Visitor Experience · חוויית המבקר</div>
      <h2 class="heading">
        The Journey —<br /><em>המסע</em>
      </h2>
      <div class="journey-row">
        <div class="journey-step">
          <div class="j-circle">🚪</div>
          <div class="j-label">Enter</div>
          <div class="j-desc">כניסה עם כרטיס זהב, שלט ה-GENESIS</div>
        </div>
        <div class="journey-step">
          <div class="j-circle">💡</div>
          <div class="j-label">Discover</div>
          <div class="j-desc">גילוי הויזיה, 7 האזורים, המניפסט</div>
        </div>
        <div class="journey-step">
          <div class="j-circle">✍️</div>
          <div class="j-label">Try</div>
          <div class="j-desc">שיתוף פעולה עם AI בזמן אמת</div>
        </div>
        <div class="journey-step">
          <div class="j-circle">🤝</div>
          <div class="j-label">Connect</div>
          <div class="j-desc">קהילת כותבים, 5 שפות</div>
        </div>
        <div class="journey-step">
          <div class="j-circle">🎁</div>
          <div class="j-label">Take Home</div>
          <div class="j-desc">Writing Set, כרטיס זיכרון</div>
        </div>
        <div class="journey-step">
          <div class="j-circle">🌍</div>
          <div class="j-label">Join</div>
          <div class="j-desc">הצטרפות לתנועה, share-pen.com</div>
        </div>
      </div>
      <div class="merch-row">
        <div class="merch-card">
          <div class="merch-card-title">Entry Ticket</div>
          <div class="merch-card-detail">
            כרטיס כניסה מוטבע עם foil זהב —<br />
            "The Writing Revolution Begins Here"<br />
            שומר כמזכרת של הרגע
          </div>
        </div>
        <div class="merch-card">
          <div class="merch-card-title">The Writing Set</div>
          <div class="merch-card-detail">
            מארז עט + מחברת + כרטיס מניפסט —<br />
            "Your Story. Your Voice. Your Share-Pen."<br />
            חוויית מותג מוחשית להבית
          </div>
        </div>
        <div class="merch-card">
          <div class="merch-card-title">Visitor Experience Page</div>
          <div class="merch-card-detail">
            עמוד חוויית מבקר מקיף —<br />
            מפת האזורים, הניווט, ה-FAQ<br />
            פיזי + דיגיטלי באחד
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ══════════════════════════════════════ SLIDE 6 ══════════════════════════════════════ -->
  <div class="slide" id="slide-6">
    <div class="slide-inner">
      <div class="slide-num">06 / 06</div>
      <div class="eyebrow">AI Tools &amp; Conclusion · כלי AI ומסקנות</div>
      <h2 class="heading">
        Living the Brand —<br /><em>חיים את המותג</em>
      </h2>
      <div class="ai-tools-row">
        <div class="ai-tool">
          <div class="ai-tool-icon">⚡</div>
          <div class="ai-tool-name">Claude Code</div>
          <div class="ai-tool-role">Partner &amp; Architect — כלי ראשי, כתיבה, קוד, אסטרטגיה</div>
        </div>
        <div class="ai-tool">
          <div class="ai-tool-icon">🎨</div>
          <div class="ai-tool-name">Canva AI</div>
          <div class="ai-tool-role">Design &amp; Visuals — עיצוב ויזואלי, מצגות, פוסטרים</div>
        </div>
        <div class="ai-tool">
          <div class="ai-tool-icon">💬</div>
          <div class="ai-tool-name">ChatGPT</div>
          <div class="ai-tool-role">Drafts &amp; Iteration — טיוטות ראשוניות, גיוון רעיונות</div>
        </div>
      </div>
      <div class="learning-block">
        <p>
          כיצד נעשה שימוש ב-AI: <strong>שותף, לא מחליף</strong> — בדיוק כמו שShare-Pen מבטיח.
          השתמשנו ב-AI לספר סיפור על שימוש ב-AI.
          <strong>The medium IS the message.</strong>
          <br />
          כלל לימוד המפתח: כשאתה חי את המסר של המותג שלך — הוא הופך לאמין.
        </p>
      </div>
      <div class="final-quote">
        <div class="gold-rule" style="margin:1.4rem auto;"></div>
        <div class="quote-text">
          "The revolution will be <em>co-written</em>."
        </div>
        <div style="margin-top:0.5rem; font-size:0.9rem; color:var(--muted); font-style:italic; font-family:'Cormorant Garamond',serif;">
          המהפכה תיכתב — ביחד.
        </div>
        <a href="https://share-pen.com" class="cta-link" target="_blank">share-pen.com →</a>
      </div>
    </div>
  </div>

</div><!-- /deck -->

<!-- ══ NAVIGATION ══ -->
<nav class="nav" role="navigation" aria-label="Slide navigation">
  <button class="nav-btn" id="btn-prev" aria-label="Previous slide" disabled>&#8592;</button>
  <div class="dots" id="dots"></div>
  <div class="counter" id="counter">1 / 6</div>
  <button class="nav-btn" id="btn-next" aria-label="Next slide">&#8594;</button>
</nav>

<script>
  const slides = Array.from(document.querySelectorAll('.slide'));
  const btnPrev = document.getElementById('btn-prev');
  const btnNext = document.getElementById('btn-next');
  const counter = document.getElementById('counter');
  const dotsContainer = document.getElementById('dots');
  const TOTAL = slides.length;
  let current = 0;

  // Build dots
  slides.forEach((_, i) => {
    const d = document.createElement('div');
    d.className = 'dot' + (i === 0 ? ' active' : '');
    d.addEventListener('click', () => goTo(i));
    dotsContainer.appendChild(d);
  });

  function goTo(next) {
    if (next === current) return;
    const prev = current;
    slides[prev].classList.remove('active');
    slides[prev].classList.add('exit-left');
    setTimeout(() => slides[prev].classList.remove('exit-left'), 600);
    slides[next].classList.add('active');
    dotsContainer.children[prev].classList.remove('active');
    dotsContainer.children[next].classList.add('active');
    current = next;
    counter.textContent = (current + 1) + ' / ' + TOTAL;
    btnPrev.disabled = current === 0;
    btnNext.disabled = current === TOTAL - 1;
  }

  btnPrev.addEventListener('click', () => { if (current > 0) goTo(current - 1); });
  btnNext.addEventListener('click', () => { if (current < TOTAL - 1) goTo(current + 1); });

  document.addEventListener('keydown', e => {
    if (e.key === 'ArrowRight' || e.key === 'ArrowDown') {
      if (current < TOTAL - 1) goTo(current + 1);
    } else if (e.key === 'ArrowLeft' || e.key === 'ArrowUp') {
      if (current > 0) goTo(current - 1);
    }
  });

  // Touch / swipe support
  let touchStartX = 0;
  document.addEventListener('touchstart', e => { touchStartX = e.changedTouches[0].clientX; }, { passive: true });
  document.addEventListener('touchend', e => {
    const dx = e.changedTouches[0].clientX - touchStartX;
    if (Math.abs(dx) > 50) {
      // RTL: swipe left = next (visually forward in RTL)
      if (dx < 0 && current < TOTAL - 1) goTo(current + 1);
      if (dx > 0 && current > 0) goTo(current - 1);
    }
  }, { passive: true });
</script>
</body>
</html>
